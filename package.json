{
  "name": "@justgains/forge",
  "version": "1.0.0",
  "description": "Forge: a faithful reproduction of Fitbod's on-device workout generator, with JustGains' improvements on top. Deterministic, offline, fully traceable.",
  "license": "GPL-3.0-only",
  "type": "module",
  "main": "src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "test": "vitest run",
    "example": "bun examples/generate.ts",
    "workshop": "bun workshop/index.ts",
    "sync": "bun scripts/sync-from-monorepo.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "convert": "^5.8.0"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "typescript": "^5.9.0",
    "vitest": "^4.1.5"
  }
}
